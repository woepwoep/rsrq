.. include:: ../../Includes.rst.txt

.. _filters:

Filters
=======

A filter is able to search only inside the results of a previously constructed query (or better, it's used to restrict the rows retrieved by an existing query). In our case we select the previously made “list query”.

Go to tab "Filter"
